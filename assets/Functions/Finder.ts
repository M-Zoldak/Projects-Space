import { AppType } from "../interfaces/EntityTypes/AppType";
import { CurrentUserType } from "../interfaces/EntityTypes/UserType";

// export const findAppUserRole = (app: AppType, currentUser: CurrentUserType) => {
//   let userInCurrentApp = app.users.find((user) => user.id == currentUser.id);
//   return userInCurrentApp.appRoles.find(
//     (appRole) => appRole.id == currentUser.currentAppRoleId
//   );
// };
